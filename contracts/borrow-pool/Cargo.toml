[package]
name = "borrow-pool"
description = "Stellar Shield borrow pool contract (skeleton)"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }
